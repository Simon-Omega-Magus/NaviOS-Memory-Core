The command line and Python interfaces can also triangulate several query formulations. Here, a deterministic demo indexes four synthetic documents into fourteen cells and thirty-eight typed edges. Every returned fragment explains exactly where it came from. The demo never reads the judge's own files and needs no API key.
