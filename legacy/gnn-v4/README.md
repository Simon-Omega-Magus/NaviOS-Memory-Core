# Legacy GNN v4 Research

This directory preserves the repository's April 2026 GraphSAGE, hardlink, Brain-Matter, and Wisp experiments for historical provenance.

It is not the supported Build Week runtime. The files contain incomplete contracts, undeclared external dependencies, and claims that were not validated end to end. Use the repository root demo and `plugins/navios-memory-reflex/` for the tested release.
